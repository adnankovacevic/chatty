import 'package:flutter/material.dart';

class MentionsPage extends StatelessWidget {
  const MentionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mentions Page'),
      ),
    );
  }
}
